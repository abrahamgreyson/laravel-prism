<?php

return [
    /*
    |--------------------------------------------------------------------------
    | JSON 资源禁用包装 (JSON Resource Without Wrapping)
    |--------------------------------------------------------------------------
    |
    | 启用后，JSON 资源将不会被包装在 'data' 键中，
    | 直接返回资源内容，提供更简洁的 API 响应格式。
    |
    */
    'json_resource_without_wrapping' => true,

    /*
    |--------------------------------------------------------------------------
    | 不可变日期 (Immutable Date)
    |--------------------------------------------------------------------------
    |
    | 启用后，模型日期字段和 Date Facade 将返回 Carbon 不可变实例，
    | 避免日期操作时的意外修改。
    |
    */
    'immutable_date' => true,

    /*
    |--------------------------------------------------------------------------
    | 统一格式的响应
    |--------------------------------------------------------------------------
    |
    | 启用后，将发布并配置 jiannei/laravel-response 包，
    | 提供统一格式的 API 响应。
    |
    */
    'unified_response' => true,

    /*
    |--------------------------------------------------------------------------
    | 模型严格模式 (Model Strict Mode)
    |--------------------------------------------------------------------------
    |
    | 启用后，将开启 Eloquent 模型的严格模式，包括：
    | - 防止懒加载 (preventLazyLoading)
    | - 防止静默丢弃属性 (preventSilentlyDiscardingAttributes)
    | - 防止访问不存在的属性 (preventAccessingMissingAttributes)
    |
    | 注意：在生产环境中会自动禁用懒加载检查以避免影响用户体验。
    |
    */
    'model_strict' => true,

    /*
    |--------------------------------------------------------------------------
    | 解除模型保护 (Unguard Models)
    |--------------------------------------------------------------------------
    |
    | 启用后，将解除所有模型的批量赋值保护，无需定义 $fillable 数组。
    | 请确保在控制器中正确验证输入数据，避免安全风险。
    |
    */
    'unguard_models' => true,

    /*
    |--------------------------------------------------------------------------
    | 禁止破坏性命令 (Prohibit Destructive Commands)
    |--------------------------------------------------------------------------
    |
    | 启用后，将在生产环境中禁止执行破坏性数据库命令，如：
    | - migrate:fresh, migrate:refresh, migrate:reset
    | - migrate:rollback, db:wipe
    |
    */
    'prohibit_destructive_commands' => true,

    /*
    |--------------------------------------------------------------------------
    | Telescope 配置 (Telescope Configuration)
    |--------------------------------------------------------------------------
    |
    | 配置 Laravel Telescope 的安装和使用。
    | - 'auto_register': 是否自动注册 Telescope 服务提供者
    | - 'environment': 安装环境 ('local' 仅本地环境, 'production' 仅生产环境, 'all' 所有环境)
    | - 'auto_prune': 是否自动配置数据清理任务
    |
    */
    'telescope' => [
        'environment' => 'local', // 'local', 'production' 或 'all'
        'auto_register' => true,
        'auto_prune' => true,
        'prune_hours' => 24,
    ],
];
